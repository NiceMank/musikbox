{
  "name": "musikbox-mobile",
  "version": "2.0.0",
  "private": true,
  "description": "MusikBox Aether Android wrapper (Capacitor)",
  "scripts": {
    "build:web": "echo 'www/ is static — nothing to build'",
    "cap:sync": "npx cap sync android",
    "cap:add": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/preferences": "^7.0.4",
    "aether-media": "file:./aether-media",
    "capacitor-music-controls-plugin": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
